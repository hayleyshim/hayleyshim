name: Update Tistory Latest Posts

on:
  schedule:
    # 매일 00:00 UTC에 실행 (한국 시간으로 오전 9시)
    - cron: '0 0 * * *' 
  workflow_dispatch: # GitHub Actions 탭에서 수동으로 실행 가능

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/blog-post-workflow@v1
        with:
          # Tistory RSS 피드 주소
          feed_url: 'https://gettingconnected.tistory.com/rss' 
          
          # 👇 특별 저장소이므로 기본 경로 'README.md' 그대로 사용합니다.
          readme_path: 'README.md' 
          
          # README.md 파일 내에서 최신 글 목록이 삽입될 마커의 이름
          comment_tag_name: 'TISTORY_LATEST_POSTS' 
          
          # 표시할 최대 게시글 수
          max_post_count: 5 
          
          # GitHub 토큰
          token: ${{ secrets.GITHUB_TOKEN }}
